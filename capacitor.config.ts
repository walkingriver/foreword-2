import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.walkingriver.foreword',
  appName: 'foreword-2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
